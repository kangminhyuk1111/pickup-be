# ?????? ??
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/shop_db}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:1234}

# JPA ??
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL:true}

# ??? ?? ??
spring.thymeleaf.cache=${THYMELEAF_CACHE:false}

# ?? ??
logging.level.root=${LOG_LEVEL:INFO}
logging.level.org.hibernate.SQL=${HIBERNATE_LOG_LEVEL:DEBUG}
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=${HIBERNATE_BINDING_LOG_LEVEL:TRACE}

# GitHub OAuth ??
github.client.id=${GITHUB_CLIENT_ID}
github.client.secret=${GITHUB_CLIENT_SECRET}
github.redirect.uri=${GITHUB_REDIRECT_URI:http://localhost:8080/auth/callback/github}

# Google OAuth ??
google.client.id=${GOOGLE_CLIENT_ID}
google.client.secret=${GOOGLE_CLIENT_SECRET}
google.redirect.uri=${GOOGLE_REDIRECT_URI:http://localhost:8080/auth/callback/google}

# JWT ?? (? ?? ?? ?? ??)
security.jwt.token.secret-key=your_super_secret_jwt_key_min_32_characters_12345678901234567890
security.jwt.token.expire-length=3600000
jwt.secret.key=your_super_secret_jwt_key_min_32_characters_12345678901234567890
jwt.expire.length=3600000

# CORS ??
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:8080}

# ?? ??
csrf.enabled=${CSRF_ENABLED:true}